<?xml version="1.0" encoding="UTF-8"?>

<beans
    xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:p="http://www.springframework.org/schema/p"
    xmlns:task="http://www.springframework.org/schema/task"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/task
http://www.springframework.org/schema/task/spring-task.xsd"
    >
    
    <bean id="schedulerFactoryBean" class="org.akaza.openclinica.job.OpenClinicaSchedulerFactoryBean" 
            depends-on="liquibase" destroy-method="destroy">
        <property name="dataSource" ref="dataSource"/>
        <property name="transactionManager" ref="transactionManager"/>
        <property name="quartzProperties">
        	<props>
        		<prop key="org.quartz.jobStore.misfireThreshold">${org.quartz.jobStore.misfireThreshold}</prop>
				<prop key="org.quartz.jobStore.class">${org.quartz.jobStore.class}</prop>
				<prop key="org.quartz.jobStore.driverDelegateClass">${org.quartz.jobStore.driverDelegateClass}</prop>
				<prop key="org.quartz.jobStore.useProperties">${org.quartz.jobStore.useProperties}</prop>
        		<prop key="org.quartz.jobStore.tablePrefix">${org.quartz.jobStore.tablePrefix}</prop>
        		<prop key="org.quartz.threadPool.class">org.quartz.simpl.SimpleThreadPool</prop>
				<prop key="org.quartz.threadPool.threadCount">${org.quartz.threadPool.threadCount}</prop>
				<prop key="org.quartz.threadPool.threadPriority">${org.quartz.threadPool.threadPriority}</prop>
        		
        	</props>
        </property>
        
        <property name="applicationContextSchedulerContextKey" value="applicationContext"/>
        
        <property name="globalJobListeners">
            <list>
                <bean class="org.akaza.openclinica.job.JobExecutionExceptionListener"/>
            </list>
        </property>
        <property name="globalTriggerListeners">
            <list>
                <bean class="org.akaza.openclinica.job.JobTriggerListener"/>
            </list>
        </property>
        <property name="triggers">
            <list>
                <ref bean="notificationOutboxTrigger" />
            </list>
        </property>
    </bean>

    <bean id="notificationOutboxJobDetail" class="org.springframework.scheduling.quartz.JobDetailBean">
        <property name="jobClass" value="org.akaza.openclinica.job.NotificationOutboxJob" />
        <property name="name" value="NotificationOutboxJob" />
        <property name="group" value="DEFAULT" />
    </bean>

    <bean id="notificationOutboxTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerBean">
        <property name="jobDetail" ref="notificationOutboxJobDetail" />
        <property name="startDelay" value="10000" />
        <property name="repeatInterval" value="60000" />
    </bean>

</beans>
